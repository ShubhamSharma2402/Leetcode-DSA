ceil in a bst is the closest value to the given val, so that ceil>=val.


approach:-

class Solution{
    public:
    int findceil(TreeNode* root,int key){
        int ceil=-1;
        while(root!=NULL){
            if(root->val==key){
             ceil=root->val;
             return ceil;
            }
            else if(root->val>key){
                ceil=root->val;
                root=root->left;
            }
            else{
                root=root->right;
            }
        }
        return ceil;
    }
}