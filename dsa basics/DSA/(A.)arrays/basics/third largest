// so isme 3 variables lene hai but most imp thing is ki tumhe INT_MIN nahi lena 
// INT_MIN ki value lowest value jo integer hold kar sakta hai , so jab hum
 //last me INT_MAIN comapre karneg toh vo hamesha true hogi 
//isliye hum long long (LLONG) use karenge 


question :-  Given an integer array nums, return the third d
istinct maximum number in this array. If the third maximum does
 not exist, return the maximum number.


4class Solution {
public:
    int thirdMax(vector<int>& nums) {
        long long first=LLONG_MIN;
        long long second =LLONG_MIN;
       long long third= LLONG_MIN;
        for(int i=0;i<nums.size();i++){
          if(nums[i]>first){
            third=second;
            second=first;
            first=nums[i];
          }
          else if(nums[i]<first && nums[i]>second){

          third=second;
          second=nums[i];
        }
        else if(nums[i]<second && nums[i]>third){
            third=nums[i];

        }
        }
        if(third == LLONG_MIN){
            return first;
        }
        else {
            return third;
        }
        
    }
};