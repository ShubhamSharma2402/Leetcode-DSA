🔹 Pattern 7: Reverse Traversal Pointers
👉 Process from the end

Questions
844. Backspace String Compare

leetcode:-844. Backspace String Compare
ques:-
Given two strings s and t, return true if they are equal when both are typed into empty text editors. '#' means a backspace character.
Note that after backspacing an empty text, the text will continue empty.

Example 1:

Input: s = "ab#c", t = "ad#c"
Output: true
Explanation: Both s and t become "ac".

approach:-(vector or stack appoach)
class Solution {
public:
    bool backspaceCompare(string s, string t) {
        vector<char> ss,tt;
        for(int i=0;i<s.size();i++){
            if(s[i]=='#'){
          if(!ss.empty()){
            ss.pop_back();
          }
            }
            else{
                ss.push_back(s[i]);
            }
        }
        for(int i=0;i<t.size();i++){
            if(t[i]=='#'){
           if(!tt.empty()){
            tt.pop_back();
           }
            }
            else{
                tt.push_back(t[i]);
            }
        }
        if(ss==tt){
            return true;
        }
        return false;
    }
};

approach2:-(two pointers (advanced))
class Solution {
public:
    bool backspaceCompare(string s, string t) {
        int i = s.size()-1, j = t.size()-1;
        
        while(i>=0 || j>=0){
            int skipS=0, skipT=0;
            
            while(i>=0 && (s[i]=='#' || skipS>0)){
                if(s[i]=='#') skipS++;
                else skipS--;
                i--;
            }
            
            while(j>=0 && (t[j]=='#' || skipT>0)){
                if(t[j]=='#') skipT++;
                else skipT--;
                j--;
            }
            
            char c1 = i>=0 ? s[i] : '\0';
            char c2 = j>=0 ? t[j] : '\0';
            if(c1 != c2) return false;
            i--; j--;
        }
        return true;
    }
};


